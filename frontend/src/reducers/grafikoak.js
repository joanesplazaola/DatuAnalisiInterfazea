import {} from "../actions/types";

const initialState = {
    proiektua: null,
    aldagaiak: [],
    ezaugarriak: [],
}

export default function (state = initialState, action) {

    switch (action.type) {

        default:
            return state;

    }

}
